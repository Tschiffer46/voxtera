FROM node:20-alpine

WORKDIR /app

# Install tsx for TypeScript hot reload
RUN npm install -g tsx

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 3001

CMD ["npx", "nodemon", "--watch", "src", "--ext", "ts", "--exec", "node --import tsx/esm src/index.ts"]
